Write an HTML code for a full-fledged AI chatbot website where the user can ask anything and everything and it will provide response in raw markdown and And after every message there will be a copy button. And yeah, another thing is that this should have a raw look. I mean as it will provide a raw output, I mean no filtering, no nothing. It will just be without any prompt a chatbot which will act as the users say. won't be any prompt okay no prompt at all for the no system prompt at all for the chat bot so it'll work it should work naturally and in its true raw form and what I want to implement in this chat bot is that I will use two kinds of API keys here one will be from Gemini and another kind will be from grok there will be two sides to this admin panel and the user panel okay and I will use firebase as the database and the database will be used for storing all the API keys saved on the admin panel I mean there won't be any environment variables and things I guess there will be environment variables but the thing is that I won't put API keys and model names in the admin I mean in the environment variables inside the admin panel the admin will be able to edit or create update delete everything in the admin panel all the for all the API keys and all the AI models that the admin wants to show to the users the users will use the API keys and the models but will not have access to change them so yeah there goes that admin will have the preference to choose in which order he wants to show the models to the user and admin will be able to add all the API keys and models in his own admin panel by separating with comma so that he can order them as he wants all the api keys will be separated by comma when added and all the models will be separated by comma so why am i adding multiple api keys because i want to integrate a system where it will automatically switch between api keys when one faces error the next switch over to the next one so this will happen when the second one faces any error it will switch to the third one this way it go and want this logic implemented there and the user will be able to save multiple conversations I mean multiple chats if they want and the user should be able to add attachments any kind of attachments could be text attachments or image attachments anything any kind of file will be allowed all file types allowed and yeah for speech to text I need a button there for voice typing so speech to text that the user will be able to do using the models and the API I mean no not the models and API keys but there will be separate kind of models model choices and API keys for speech to text also so for normal text generation I mean for the whole AI to work it will use the text generation models and for speech to text there will be separate list of models so user will be able to choose from both of them suppose the one speech-to-text model is not working then another will work and suppose one text generation model is not working then another will work this way the same will go and I want the user to be able to export their chat or copy the whole chat as markdown if they want and I want copy I want a copy button below each and every message for the ease of copying and when the user logs in or signs up there will be email and password to do that and for seeing the password they can click on the view button on the password field yeah these are the things that i want in this website so kindly write the html code for me 